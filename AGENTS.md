# Agent Instructions

- After each attempt to fix the SimpleClouds + Oculus + Distant Horizons cloud rendering issue, record what changed, why it was attempted, and the observed/resulting behavior in `simpleclouds_dh_clouds_missing_notes.md`.
- Before suggesting or applying a new fix for that issue, read `simpleclouds_dh_clouds_missing_notes.md` and verify whether the same approach was already attempted.
- If an approach was already attempted, do not repeat it unless there is new evidence that changes the reasoning; document that new evidence in `simpleclouds_dh_clouds_missing_notes.md`.
